{
  "$comment": "Lighthouse CI budget config, Phase 11 slices #63 + #64. The workflow at .github/workflows/lighthouse.yml (currently .yml.disabled while the GitHub Actions billing block clears) runs treosh/lighthouse-ci-action against three URLs and asserts the public budget below. Each URL is appended to the Vercel preview origin at workflow runtime; the action's `numberOfRuns: 3` smooths out cold-start noise.",
  "ci": {
    "collect": {
      "numberOfRuns": 3,
      "settings": {
        "preset": "desktop"
      }
    },
    "assert": {
      "assertions": {
        "categories:performance": ["error", { "minScore": 0.95 }],
        "categories:accessibility": ["error", { "minScore": 0.95 }],
        "categories:best-practices": ["error", { "minScore": 0.95 }],
        "categories:seo": ["error", { "minScore": 0.95 }]
      }
    },
    "upload": {
      "target": "temporary-public-storage"
    }
  }
}
